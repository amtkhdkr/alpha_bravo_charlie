[defaults]
# Turn off host_key_checking prompt
host_key_checking = False
# Gather facts using cache mechanism
gathering = smart
# Cache facts to JSON
fact_caching = jsonfile
fact_caching_connection = /tmp/facts_cache
